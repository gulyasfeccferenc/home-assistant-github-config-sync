# Home Assistant Config Sync

Safe bidirectional Git synchronization for Home Assistant configuration.

The app preserves local Home Assistant changes, synchronizes remote Git changes using fast-forward-only updates, validates Home Assistant configuration before applying changes, and stops automatically when local and remote histories conflict.
